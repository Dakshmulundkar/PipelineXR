# Fallback nixpacks config — Railway uses Dockerfile (see railway.json)
[phases.setup]
nixPkgs = ["nodejs_20", "git", "trivy"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "node server/index.js"
