[build]
  command = "cd client && npm install && npm run build"
  publish = "client/dist"

[functions]
  directory = "client/netlify/functions"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
